//Struct of program
#include<iostream>// input , output stream --> 입출력 stream 'processor directive'
using namespace std; // inout, output fuctuon 앞에 일일이 넣어야 하지만 앞에 통합시켜 놓음.

int main()
{
	int x = 2; // int x에 2라는 값을 저장 하였다.
	int y = x + 3;// x,y는 literal 이라고도 부른다.

	cout << y << endl;

	return 0;

	/*
	표현식과 명령문의 차이는 무엇인가?
	함수와 라이비러리의 차이는 무엇인가?
	C++에서 문장을 끝내는 기호는 무엇인가?
	문법 오류란 무엇인가?
	*/
}